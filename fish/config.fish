set -g GREP_OPTIONS '--color=always'
# Set dir colors
#eval ( dircolors --c-shell $HOME/dotfiles/.dircolors.ansi-dark )
#eval ( dircolors --c-shell $HOME/.dir_colors/dircolors )
eval ( dircolors --c-shell $HOME/.dircolors.256dark )
